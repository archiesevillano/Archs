import PageNotAvailable from "@/components/PageNotAvailable";

const Accessibility = () => {
    return (
        <div className="accessibility w-full h-screen flex flex-row items-center justify-center">
            <PageNotAvailable />
        </div>
    );
}

export default Accessibility;